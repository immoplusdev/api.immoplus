meta {
  name: read-residence
  type: http
  seq: 2
}

get {
  url: {{API_URL}}/biens-immobiliers/{{DEFAULT_BIEN_IMMOBILIER_ID}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{proToken}}
}

tests {
  test("Should receive a not null id", function() {
    const {data: { id }} = res.getBody();
  
    expect(id).to.be.a('string');
  });
}
