meta {
  name: send-email-otp
  type: http
  seq: 6
}

post {
  url: {{API_URL}}/auth/send-email-otp
  body: json
  auth: bearer
}

auth:bearer {
  token: {{customerToken}}
}

body:json {
  {
    "email": "{{customerEmail}}"
  }
}

tests {
  test("Should return 204 status code", function() {
    const status = res.getStatus();
    
    expect(status == 204).to.be.equal(true);
  });
  
}
