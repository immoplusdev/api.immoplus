import { OmitMethods } from '@/lib/ts-utilities';
import { WrapperResponseDto } from "@/lib/responses";
import { ApiProperty } from "@nestjs/swagger";

export class {{classify name}}CommandResponse {
  constructor(data?: OmitMethods<{{classify name}}CommandResponse>) {
    if(data) Object.assign(this, data);
  }
}

  export class WrapperResponse{{classify name}}CommandResponseDto extends WrapperResponseDto<{{classify name}}CommandResponse> {
    @ApiProperty({ type: {{classify name}}CommandResponse })
    data: {{classify name}}CommandResponse;
  }

