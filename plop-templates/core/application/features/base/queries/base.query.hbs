import { OmitMethods } from "@/core/domain/shared/ts-utilities";
export class {{classify name}}Query {
  constructor(data?: OmitMethods<{{classify name}}Query>) {
    if(data) Object.assign(this, data);
  }
}
