import { IMapper, OmitMethods } from "@/lib/ts-utilities";
import { {{classify name}} } from "@/core/domain/{{dasherize group}}";
import { Create{{classify name}}Dto } from "./create-{{dasherize name}}.dto";


export class Create{{classify name}}DtoMapper implements IMapper<Create{{classify name}}Dto, {{classify name}}> {
  mapFrom(object: OmitMethods<Create{{classify name}}Dto>): {{classify name}} {
    return new {{classify name}}(object as never);
  }

  mapTo(object: Partial<{{classify name}}>): Create{{classify name}}Dto {
    return new Create{{classify name}}Dto(object as never);
  }
}
