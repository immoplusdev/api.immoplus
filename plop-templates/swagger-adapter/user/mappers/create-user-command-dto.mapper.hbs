import { IMapper } from 'clever-ts-utilities';
import { {{classify name}}Dto } from '../{{name}}.dto';
import { {{classify name}} } from 'src/core/domain/models/{{name}}';
import { Create{{classify name}}Dto } from '../create-{{name}}-command.dto';

export class Create{{classify name}}DtoMapper
  implements IMapper<{{classify name}}, Create{{classify name}}Dto>
{
  mapFrom(param: {{classify name}}): Create{{classify name}}Dto {
    return new Create{{classify name}}Dto({
      //login: param.login,
      //email: param.email,
      //phone: param.phone,
      //avatar: param.avatar,
      //password: param.password,
    });
  }

  mapTo(param: Create{{classify name}}Dto): {{classify name}} {
    return new {{classify name}}({
      //id: null,
      //login: param.login,
      //email: param.email,
      //phone: param.phone,
      //avatar: param.avatar,
      //password: param.password,
    });
  }
}
