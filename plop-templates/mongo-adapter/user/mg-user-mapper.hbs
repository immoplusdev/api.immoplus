import { IMapper } from "clever-ts-utilities";
import { {{classify name}} } from "@/core/domain/models/{{name}}";
import { Mg{{classify name}} } from "./mg-{{name}}.schema";

export class Mg{{classify name}}Mapper implements IMapper<{{classify name}}, Mg{{classify name}}> {
  mapFrom(param: {{classify name}}): Mg{{classify name}} {
    return new Mg{{classify name}}({
    });
  }

  mapTo(param: Mg{{classify name}}): {{classify name}} {
    return new {{classify name}}({
      // id: (param as any)._id,
    });
  }
}
