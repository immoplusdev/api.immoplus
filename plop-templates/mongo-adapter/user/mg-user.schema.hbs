import { Prop, Schema, SchemaFactory } from '@nestjs/mongoose';
import { HydratedDocument } from 'mongoose';

export type Mg{{classify name}}Document = HydratedDocument<Mg{{classify name}}>;

@Schema({ collection: '{{name}}s' })
export class Mg{{classify name}} {
  // @Prop()
  // login: string;


  constructor(partial?: Partial<Mg{{classify name}}>) {
    if (partial) Object.assign(this, partial);
  }
}

export const Mg{{classify name}}Schema = SchemaFactory.createForClass(Mg{{classify name}});
