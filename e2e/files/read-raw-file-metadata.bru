meta {
  name: read-raw-file-metadata
  type: http
  seq: 2
}

get {
  url: {{API_URL}}/files/{{DEFAULT_FILE_ID}}
  body: multipartForm
  auth: bearer
}

auth:bearer {
  token: {{DEFAULT_CUSTOMER_AT}}
}
